services:
  - type: web
    name: digit-recognizer-api
    runtime: python
    rootDir: digit-recognizer/backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
